services:
  aidrama:
    build: .
    env_file:
      - .env
    ports:
      - "5679:5679"
    volumes:
      - ./data:/app/data
    environment:
      - NODE_ENV=production
      - PORT=5679
    restart: unless-stopped
